<%= form_for @listing, html: { multipart: true } do |f| %>
  <% if @listing.errors.any? %>
    <% @listing.errors.full_messages.each do |message| %>
        <p><%= message %></p>
    <% end %>
  <% end %>
  <div>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div>
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div>
    <%= f.label :price%><br />
    <%= f.number_field :price %>
  </div>
  <div>
    <%= f.label :expiration %><br />
    <%= f.datetime_field :expiration, min: Date.today%>
  </div>
  <div>
    <%= f.label :category_id %><br />
    <%= f.collection_select :category_id, Category.all, :id, :name, prompt: "Add a catgeory" %>
  </div>
  <div>
    <%= f.label :condition_id %><br />
    <%= f.collection_select :condition_id, Condition.all, :id, :name, prompt: "Add a condition" %>
  </div>
  <div>
    <%= f.label :image %></br>
    <%= f.file_field :image, id: 'pictureInput' %>
  </div>
  <div id="target">
  </div>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <div>
    <%= f.submit "Submit" %>
  </div>
<% end %>
